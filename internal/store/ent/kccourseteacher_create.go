// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"tkserver/internal/store/ent/kccourse"
	"tkserver/internal/store/ent/kccourseteacher"
	"tkserver/internal/store/ent/teacher"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// KcCourseTeacherCreate is the builder for creating a KcCourseTeacher entity.
type KcCourseTeacherCreate struct {
	config
	mutation *KcCourseTeacherMutation
	hooks    []Hook
}

// SetShowStatus sets the "show_status" field.
func (kctc *KcCourseTeacherCreate) SetShowStatus(u uint8) *KcCourseTeacherCreate {
	kctc.mutation.SetShowStatus(u)
	return kctc
}

// SetNillableShowStatus sets the "show_status" field if the given value is not nil.
func (kctc *KcCourseTeacherCreate) SetNillableShowStatus(u *uint8) *KcCourseTeacherCreate {
	if u != nil {
		kctc.SetShowStatus(*u)
	}
	return kctc
}

// SetSortOrder sets the "sort_order" field.
func (kctc *KcCourseTeacherCreate) SetSortOrder(i int) *KcCourseTeacherCreate {
	kctc.mutation.SetSortOrder(i)
	return kctc
}

// SetNillableSortOrder sets the "sort_order" field if the given value is not nil.
func (kctc *KcCourseTeacherCreate) SetNillableSortOrder(i *int) *KcCourseTeacherCreate {
	if i != nil {
		kctc.SetSortOrder(*i)
	}
	return kctc
}

// SetCourseID sets the "course_id" field.
func (kctc *KcCourseTeacherCreate) SetCourseID(i int) *KcCourseTeacherCreate {
	kctc.mutation.SetCourseID(i)
	return kctc
}

// SetNillableCourseID sets the "course_id" field if the given value is not nil.
func (kctc *KcCourseTeacherCreate) SetNillableCourseID(i *int) *KcCourseTeacherCreate {
	if i != nil {
		kctc.SetCourseID(*i)
	}
	return kctc
}

// SetTeacherID sets the "teacher_id" field.
func (kctc *KcCourseTeacherCreate) SetTeacherID(i int) *KcCourseTeacherCreate {
	kctc.mutation.SetTeacherID(i)
	return kctc
}

// SetNillableTeacherID sets the "teacher_id" field if the given value is not nil.
func (kctc *KcCourseTeacherCreate) SetNillableTeacherID(i *int) *KcCourseTeacherCreate {
	if i != nil {
		kctc.SetTeacherID(*i)
	}
	return kctc
}

// SetTeacher sets the "teacher" edge to the Teacher entity.
func (kctc *KcCourseTeacherCreate) SetTeacher(t *Teacher) *KcCourseTeacherCreate {
	return kctc.SetTeacherID(t.ID)
}

// SetCourse sets the "course" edge to the KcCourse entity.
func (kctc *KcCourseTeacherCreate) SetCourse(k *KcCourse) *KcCourseTeacherCreate {
	return kctc.SetCourseID(k.ID)
}

// Mutation returns the KcCourseTeacherMutation object of the builder.
func (kctc *KcCourseTeacherCreate) Mutation() *KcCourseTeacherMutation {
	return kctc.mutation
}

// Save creates the KcCourseTeacher in the database.
func (kctc *KcCourseTeacherCreate) Save(ctx context.Context) (*KcCourseTeacher, error) {
	var (
		err  error
		node *KcCourseTeacher
	)
	kctc.defaults()
	if len(kctc.hooks) == 0 {
		if err = kctc.check(); err != nil {
			return nil, err
		}
		node, err = kctc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*KcCourseTeacherMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = kctc.check(); err != nil {
				return nil, err
			}
			kctc.mutation = mutation
			node, err = kctc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(kctc.hooks) - 1; i >= 0; i-- {
			mut = kctc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, kctc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (kctc *KcCourseTeacherCreate) SaveX(ctx context.Context) *KcCourseTeacher {
	v, err := kctc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (kctc *KcCourseTeacherCreate) defaults() {
	if _, ok := kctc.mutation.ShowStatus(); !ok {
		v := kccourseteacher.DefaultShowStatus
		kctc.mutation.SetShowStatus(v)
	}
	if _, ok := kctc.mutation.SortOrder(); !ok {
		v := kccourseteacher.DefaultSortOrder
		kctc.mutation.SetSortOrder(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (kctc *KcCourseTeacherCreate) check() error {
	if _, ok := kctc.mutation.ShowStatus(); !ok {
		return &ValidationError{Name: "show_status", err: errors.New("ent: missing required field \"show_status\"")}
	}
	if _, ok := kctc.mutation.SortOrder(); !ok {
		return &ValidationError{Name: "sort_order", err: errors.New("ent: missing required field \"sort_order\"")}
	}
	return nil
}

func (kctc *KcCourseTeacherCreate) sqlSave(ctx context.Context) (*KcCourseTeacher, error) {
	_node, _spec := kctc.createSpec()
	if err := sqlgraph.CreateNode(ctx, kctc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (kctc *KcCourseTeacherCreate) createSpec() (*KcCourseTeacher, *sqlgraph.CreateSpec) {
	var (
		_node = &KcCourseTeacher{config: kctc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: kccourseteacher.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: kccourseteacher.FieldID,
			},
		}
	)
	if value, ok := kctc.mutation.ShowStatus(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeUint8,
			Value:  value,
			Column: kccourseteacher.FieldShowStatus,
		})
		_node.ShowStatus = value
	}
	if value, ok := kctc.mutation.SortOrder(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: kccourseteacher.FieldSortOrder,
		})
		_node.SortOrder = value
	}
	if nodes := kctc.mutation.TeacherIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   kccourseteacher.TeacherTable,
			Columns: []string{kccourseteacher.TeacherColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: teacher.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.TeacherID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := kctc.mutation.CourseIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   kccourseteacher.CourseTable,
			Columns: []string{kccourseteacher.CourseColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: kccourse.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.CourseID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// KcCourseTeacherCreateBulk is the builder for creating many KcCourseTeacher entities in bulk.
type KcCourseTeacherCreateBulk struct {
	config
	builders []*KcCourseTeacherCreate
}

// Save creates the KcCourseTeacher entities in the database.
func (kctcb *KcCourseTeacherCreateBulk) Save(ctx context.Context) ([]*KcCourseTeacher, error) {
	specs := make([]*sqlgraph.CreateSpec, len(kctcb.builders))
	nodes := make([]*KcCourseTeacher, len(kctcb.builders))
	mutators := make([]Mutator, len(kctcb.builders))
	for i := range kctcb.builders {
		func(i int, root context.Context) {
			builder := kctcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*KcCourseTeacherMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, kctcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, kctcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, kctcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (kctcb *KcCourseTeacherCreateBulk) SaveX(ctx context.Context) []*KcCourseTeacher {
	v, err := kctcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
